# Story 8.7: Automação e Orquestração FotoIA

## Status
InProgress

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** sistema,
**I want** cron jobs e regras de automação para o pipeline FotoIA operar 24/7,
**so that** o ciclo completo de vendas e entrega funcione sem intervenção humana.

## Acceptance Criteria
1. Cron job de follow-ups: verificar leads sem resposta há 24h/48h e acionar agente
2. Orquestrador de pipeline: transições automáticas de status baseadas em eventos
3. Cron job: leads sem resposta após 72h → mover para PERDIDO automaticamente
4. Alertas para o gestor: notificações de pedidos travados, erros de produção, aprovações pendentes
5. Relatórios de performance: relatório semanal automático via email/painel
6. Retry automático em falhas de geração de imagem (max 3 tentativas)
7. Queue para geração de imagens (múltiplos pedidos simultâneos sem timeout)

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [x] Criar sistema de cron jobs (AC: 1, 3)
  - [x] Criar `src/app/api/cron/fotoia/route.ts`
  - [x] Cron Vercel: executar a cada hora
  - [x] Verificar follow-ups pendentes: `createdAt < agora - 24h AND status = PROPOSTA_ENVIADA` → FOLLOWUP_1
  - [x] Verificar follow-ups 2: `createdAt < agora - 48h AND status = FOLLOWUP_1` → FOLLOWUP_2
  - [x] Verificar perdidos: `createdAt < agora - 72h AND status IN [FOLLOWUP_2, AGUARDANDO_PAGAMENTO]` → PERDIDO
- [x] Orquestrador de pipeline (AC: 2)
  - [x] Criar `src/lib/fotoia/pipeline-orchestrator.ts`
  - [x] Função `onStatusChange(pedidoId, oldStatus, newStatus)`
  - [x] Mapa de transições: cada status → ação automática → próximo agente
  - [x] Integrar com todos os agentes do Story 8.2
- [ ] Alertas para o gestor (AC: 4)
  - [ ] Verificar pedidos travados: mesmo status por > 30min (configurável)
  - [ ] Criar notificação no banco + emitir Socket.io
  - [ ] Toast/badge no painel do gestor
  - [ ] Alertas por tipo: "Produção travada", "Aprovação pendente há 2h", "Pagamento expirado"
- [ ] Relatórios de performance (AC: 5)
  - [ ] API `GET /api/foto-ia/relatorio?periodo=semana`
  - [ ] Campos: pedidos criados, pedidos concluídos, receita, taxa de conversão, tempo médio, NPS
  - [ ] Seção de relatórios no dashboard FotoIA
- [ ] Retry em falhas de geração (AC: 6)
  - [ ] Ao receber webhook de falha do Replicate: incrementar contador de tentativas
  - [ ] Se tentativas < 3: retry automático com mesmo prompt após 5 min
  - [ ] Se tentativas >= 3: status ERRO, notificar gestor
  - [ ] Campo `tentativasGeracao Int @default(0)` no PedidoFotoIA
- [x] Queue de geração (AC: 7)
  - [x] Usar tabela como queue simples: model `GeracaoQueue` no schema
  - [x] Processar até 3 gerações simultâneas por organização
  - [x] Gerações pendentes ficam em `EM_PRODUCAO` aguardando slot
  - [x] Worker verifica a cada 30s pedidos na queue (via cron)

## Dev Notes
**Depende de:** Story 8.2 (agentes), Story 8.3 (gateway), Story 8.4 (Replicate)

**Vercel Cron (vercel.json):**
```json
{
  "crons": [
    { "path": "/api/cron/fotoia", "schedule": "0 * * * *" },
    { "path": "/api/cron/atualizar-status", "schedule": "0 6 * * *" }
  ]
}
```

**Pipeline orchestrator map:**
```ts
const PIPELINE_TRANSITIONS = {
  NOVO_LEAD: () => agentVendedor.qualificarLead(pedidoId),
  PAGAMENTO_CONFIRMADO: () => agentProdutor.gerarImagens(pedidoId),
  EM_PRODUCAO: null, // aguarda webhook Replicate
  AGUARDANDO_APROVACAO: () => agentEntregador.enviarParaAprovacao(pedidoId),
  // ...
}
```

**Queue simples com tabela:**
```prisma
model GeraragemQueue {
  id        String   @id @default(cuid())
  pedidoId  String   @unique
  prioridade Int     @default(0)
  tentativas Int     @default(0)
  createdAt DateTime @default(now())
}
```

### Testing
- Testar cron move leads para PERDIDO após 72h
- Testar retry de geração após falha
- Testar queue não ultrapassa limite de simultâneos

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |
| 2026-02-23 | 1.1 | Status atualizado para InProgress; cron job, pipeline orchestrator e queue system criados | @dev |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6
### Debug Log References
### Completion Notes List
- Cron job criado em src/app/api/cron/fotoia/route.ts (follow-ups 24h/48h e leads perdidos 72h)
- Pipeline orchestrator criado em src/lib/fotoia/pipeline-orchestrator.ts com mapa de transições e integração com agentes
- Model GeracaoQueue adicionado ao schema.prisma para queue de geração de imagens
- Processamento da queue integrado ao cron job (até 3 gerações simultâneas por org)
- Alertas para gestor, relatórios de performance e retry automático pendentes
### File List
- `src/app/api/cron/fotoia/route.ts` — Cron job de follow-ups e limpeza de leads perdidos
- `src/lib/fotoia/pipeline-orchestrator.ts` — Orquestrador de transições do pipeline FotoIA
- `prisma/schema.prisma` — Model GeracaoQueue adicionado para queue de geração

## QA Results
