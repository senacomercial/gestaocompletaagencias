# Story 2.1: Schema & API — Funis, Etapas e Leads

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["prisma:migrate", "typecheck", "unit-tests"]

## Story
**As a** sistema,
**I want** os models de banco e endpoints de API para funis, etapas e leads,
**so that** os módulos de UI possam consumir dados reais.

## Acceptance Criteria
1. Prisma models: `Funil` (id, nome, descricao, organizacaoId, ordem), `EtapaFunil` (id, nome, cor, ordem, funilId), `Lead` (id, nome, email, telefone, empresa, etapaId, funilId, organizacaoId, createdAt, updatedAt)
2. API routes RESTful: `GET/POST /funis`, `GET/PUT/DELETE /funis/[id]`, CRUD `/etapas`, CRUD `/leads`, `PATCH /leads/[id]/etapa`
3. Todos os endpoints validam `organizacaoId` da sessão
4. Validação de inputs com Zod em todas as rotas
5. Migration aplicada com sucesso
6. Seed com 1 funil "Funil Principal" e 4 etapas padrão: Novo Lead, Qualificado, Proposta Enviada, Negociação

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Adicionar models ao schema Prisma (AC: 1)
  - [ ] Model `Funil`: id, nome, descricao, organizacaoId, ordem, createdAt
  - [ ] Model `EtapaFunil`: id, nome, cor, ordem, funilId (FK), createdAt
  - [ ] Model `Lead`: id, nome, email, telefone, empresa, etapaId (FK), funilId (FK), organizacaoId, createdAt, updatedAt
  - [ ] Relações: Funil hasMany EtapaFunil, Funil hasMany Lead, EtapaFunil hasMany Lead
  - [ ] Index em `organizacaoId` nos três models
- [ ] Aplicar migration (AC: 5)
  - [ ] `prisma migrate dev --name crm-base`
- [ ] Criar API routes de Funis (AC: 2, 3, 4)
  - [ ] `src/app/api/funis/route.ts` — GET (listar) + POST (criar)
  - [ ] `src/app/api/funis/[id]/route.ts` — GET + PUT + DELETE
  - [ ] Validação Zod: nome obrigatório, descricao opcional
  - [ ] Filtrar por `organizacaoId` da sessão
- [ ] Criar API routes de Etapas (AC: 2, 3, 4)
  - [ ] `src/app/api/etapas/route.ts` — GET + POST
  - [ ] `src/app/api/etapas/[id]/route.ts` — PUT + DELETE
  - [ ] Validação: não excluir etapa com leads vinculados
- [ ] Criar API routes de Leads (AC: 2, 3, 4)
  - [ ] `src/app/api/leads/route.ts` — GET + POST
  - [ ] `src/app/api/leads/[id]/route.ts` — GET + PUT + DELETE
  - [ ] `src/app/api/leads/[id]/etapa/route.ts` — PATCH (mover lead)
  - [ ] Validação Zod completa
- [ ] Atualizar seed (AC: 6)
  - [ ] Criar funil "Funil Principal"
  - [ ] Criar 4 etapas: Novo Lead, Qualificado, Proposta Enviada, Negociação

## Dev Notes
**Padrão de rota API com auth:**
```ts
import { getServerSession } from 'next-auth'
import { authOptions } from '@/lib/auth'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const session = await getServerSession(authOptions)
  if (!session) return Response.json({ error: 'Unauthorized' }, { status: 401 })
  const orgId = session.user.organizacaoId
  const funis = await prisma.funil.findMany({ where: { organizacaoId: orgId } })
  return Response.json(funis)
}
```

**Validação Zod exemplo:**
```ts
import { z } from 'zod'
const CreateFunilSchema = z.object({
  nome: z.string().min(1).max(100),
  descricao: z.string().optional(),
})
```

**Cores padrão para etapas:** `#D4AF37` (gold), `#3B82F6` (azul), `#22C55E` (verde), `#F59E0B` (âmbar)

### Testing
- Teste de integração: POST /funis cria funil com organizacaoId correto
- Teste: DELETE etapa com leads retorna erro 400
- Teste: GET /leads retorna apenas leads da organização da sessão

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
