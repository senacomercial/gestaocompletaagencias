# Story 1.2: Database & Schema Multi-tenant

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["prisma:generate", "prisma:migrate", "typecheck"]

## Story
**As a** sistema,
**I want** um banco PostgreSQL com schema Prisma inicial,
**so that** todos os dados sejam isolados por organização desde o início.

## Acceptance Criteria
1. Prisma configurado com `DATABASE_URL` via `.env`
2. Schema inicial com models: `Organizacao` (id, nome, slug, createdAt), `Usuario` (id, nome, email, senha hash, role, organizacaoId)
3. Enum `Role`: `ADMIN`, `GESTOR`, `OPERACIONAL`
4. Migration inicial aplicada com sucesso (`prisma migrate dev`)
5. Seed básico criando 1 organização + 1 usuário admin para desenvolvimento
6. Prisma Client gerado e exportado de `src/lib/prisma.ts`
7. Scripts `npm run prisma:generate`, `npm run prisma:migrate`, `npm run prisma:seed` funcionando

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Configurar Prisma com PostgreSQL (AC: 1)
  - [ ] Instalar `prisma` e `@prisma/client`
  - [ ] Criar/verificar `prisma/schema.prisma` com `datasource db`
  - [ ] Confirmar `DATABASE_URL` no `.env.example`
- [ ] Definir models no schema (AC: 2, 3)
  - [ ] Model `Organizacao`: id (cuid), nome, slug, createdAt
  - [ ] Model `Usuario`: id, nome, email, senhaHash, role, organizacaoId (FK), createdAt, updatedAt
  - [ ] Enum `Role`: ADMIN, GESTOR, OPERACIONAL
  - [ ] Relação: `Organizacao` hasMany `Usuario`
- [ ] Aplicar migration inicial (AC: 4)
  - [ ] `npx prisma migrate dev --name init`
  - [ ] Confirmar que migração roda sem erros
- [ ] Criar seed de desenvolvimento (AC: 5)
  - [ ] `prisma/seed.ts` com 1 organização "Agência Demo" + 1 usuário admin
  - [ ] Senha hasheada com bcrypt
- [ ] Exportar Prisma Client (AC: 6)
  - [ ] Criar `src/lib/prisma.ts` com singleton client
- [ ] Configurar scripts no package.json (AC: 7)
  - [ ] `prisma:generate`: `prisma generate`
  - [ ] `prisma:migrate`: `prisma migrate dev`
  - [ ] `prisma:seed`: `ts-node prisma/seed.ts`
  - [ ] `db:reset`: drop + migrate + seed

## Dev Notes
**Princípio chave:** Todo model deve ter `organizacaoId` para garantir isolamento multi-tenant.

**Schema Prisma base:**
```prisma
model Organizacao {
  id        String   @id @default(cuid())
  nome      String
  slug      String   @unique
  createdAt DateTime @default(now())
  usuarios  Usuario[]
}

model Usuario {
  id           String   @id @default(cuid())
  nome         String
  email        String   @unique
  senhaHash    String
  role         Role     @default(OPERACIONAL)
  organizacaoId String
  organizacao  Organizacao @relation(fields: [organizacaoId], references: [id])
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

enum Role {
  ADMIN
  GESTOR
  OPERACIONAL
}
```

**src/lib/prisma.ts (singleton):**
```ts
import { PrismaClient } from '@prisma/client'
const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }
export const prisma = globalForPrisma.prisma ?? new PrismaClient()
if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

### Testing
- `npm run prisma:migrate` deve completar sem erros
- `npm run prisma:seed` deve criar organização e usuário
- `npm run prisma:generate` deve gerar tipos TypeScript corretos

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
