# Story 7.4: Orquestração — Acionar Agentes & Monitorar Execuções

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** gestor,
**I want** acionar agentes e acompanhar execução em tempo real,
**so that** eu orquestre as equipes de IA manualmente quando necessário.

## Acceptance Criteria
1. Botão "Acionar Agente" em agentes DISPONÍVEL → modal de execução
2. Modal: campo de comando, contexto (lead/projeto/genérico), parâmetros adicionais
3. Ao acionar: `Execucao` criada; toast "▶ Agente acionado"
4. Painel lateral "Execuções Ativas" com progresso e logs em streaming
5. Toast de resultado ao concluir (verde) ou falhar (vermelho) com preview do output
6. Output salvo em `Execucao.output` — acessível no histórico
7. Cancelar execução PENDENTE ou EM_ANDAMENTO
8. Agente retorna a DISPONIVEL ao concluir ou falhar

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Botão e modal de acionamento (AC: 1, 2)
  - [ ] Botão "▶ Acionar" visível apenas se `status === 'DISPONIVEL'`
  - [ ] Criar `src/components/squad/AcionarAgenteModal.tsx`
  - [ ] Campo comando: textarea descrevendo o que o agente deve fazer
  - [ ] Select contexto: Lead (busca por nome), Projeto (busca), Genérico
  - [ ] Se Lead/Projeto: exibe dados do contexto selecionado
  - [ ] Parâmetros JSON opcionais (textarea)
- [ ] Criar execução (AC: 3)
  - [ ] `POST /api/execucoes` — cria Execucao com status PENDENTE
  - [ ] Emite evento Socket.io `execucao:criada` para o servidor AIOS
  - [ ] Toast "▶ Agente [nome] acionado"
  - [ ] Status agente → EM_EXECUCAO
- [ ] Painel de Execuções Ativas (AC: 4)
  - [ ] Criar `src/components/squad/ExecucoesAtivasPanel.tsx`
  - [ ] Fixed panel no lado direito da tela (sobrepõe conteúdo)
  - [ ] Lista execuções com status PENDENTE ou EM_ANDAMENTO
  - [ ] Para cada: nome do agente, comando (truncado), progresso, timestamp
  - [ ] Stream de logs via Socket.io `execucao:{execucaoId}:log`
- [ ] Toast de resultado (AC: 5)
  - [ ] Ouvir `execucao:concluida` e `execucao:falha`
  - [ ] CONCLUIDA: toast verde com preview do output (primeiros 100 chars)
  - [ ] FALHA: toast vermelho com mensagem de erro
- [ ] Salvar output (AC: 6)
  - [ ] Webhook ou Socket.io event do servidor AIOS
  - [ ] `PATCH /api/execucoes/[id]` com output e status
- [ ] Cancelar execução (AC: 7)
  - [ ] Botão "Cancelar" na linha da execução ativa
  - [ ] `POST /api/execucoes/[id]/cancelar`
  - [ ] Emite `execucao:cancelar` para o servidor AIOS
- [ ] Status do agente pós-execução (AC: 8)
  - [ ] Ao receber `execucao:concluida` ou `execucao:falha`: agente → DISPONIVEL
  - [ ] Exceção: ERRO mantém agente em ERRO

## Dev Notes
**Depende de:** Story 7.1 (models), Story 7.2 (dashboard), Story 7.3 (slide-over)

**Fluxo de comunicação:**
```
UI → POST /api/execucoes → socket emit "execucao:nova" → Servidor AIOS
Servidor AIOS → socket emit "execucao:log" → UI (streaming)
Servidor AIOS → PATCH /api/execucoes/[id] (output + status) → UI atualiza
```

**AIOS_API_KEY:** Todas as chamadas do servidor AIOS incluem header `Authorization: Bearer ${AIOS_API_KEY}`

### Testing
- Testar botão acionar aparece apenas em agente DISPONIVEL
- Testar cancelamento de execução
- Testar toast de resultado com preview do output

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
