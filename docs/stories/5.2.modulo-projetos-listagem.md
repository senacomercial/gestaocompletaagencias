# Story 5.2: Módulo Projetos — Listagem de Clientes Ativos

## Status
Draft

## Executor Assignment
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** gestor,
**I want** visualizar todos os projetos e clientes ativos em uma tela centralizada,
**so that** eu tenha controle do portfólio de clientes em andamento.

## Acceptance Criteria
1. Página `/projetos` lista projetos com `status = ATIVO`
2. Card de projeto: nome do cliente, tipo de serviço (badge dourado), data de início, progresso de tarefas (barra gold→bronze), responsável, valor mensal
3. Filtros: por tipo de serviço, por responsável, por status
4. Busca por nome do cliente
5. Alternância Grid / Lista
6. Clicar navega para `/projetos/[id]`
7. Indicador visual de projetos com tarefas atrasadas (borda vermelha)

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar página de projetos (AC: 1)
  - [ ] `src/app/(dashboard)/projetos/page.tsx`
  - [ ] Query: projetos com `status = ATIVO` da organização
  - [ ] Incluir: lead, tarefas (contagem), responsável principal
- [ ] Criar ProjetoCard (AC: 2)
  - [ ] Criar `src/components/projetos/ProjetoCard.tsx`
  - [ ] Nome do cliente (lead.empresa ou lead.nome)
  - [ ] Badge tipo de serviço: fundo gold/bronze, texto dark
  - [ ] Data de início formatada
  - [ ] Barra de progresso: `concluidas/total * 100%`, gradiente gold→bronze
  - [ ] Avatar do responsável principal
  - [ ] Valor mensal do contrato (se RECORRENTE)
- [ ] Implementar filtros (AC: 3)
  - [ ] Criar `src/components/projetos/ProjetosFiltros.tsx`
  - [ ] Select tipo de serviço (valores únicos do banco)
  - [ ] Select responsável (usuários da organização)
  - [ ] Select status (ATIVO, PAUSADO, CONCLUIDO, CANCELADO)
  - [ ] Filtros aplicados client-side (useState)
- [ ] Busca por nome (AC: 4)
  - [ ] Input de busca no header
  - [ ] Filtrar por `lead.nome` ou `lead.empresa` (case-insensitive)
  - [ ] Debounce 300ms
- [ ] Toggle Grid / Lista (AC: 5)
  - [ ] Botões de toggle no header (ícones Grid e List)
  - [ ] Grid: 3 colunas desktop, 2 tablet, 1 mobile
  - [ ] Lista: tabela com colunas: cliente, tipo, progresso, responsável, valor, ações
- [ ] Navegação para detalhe (AC: 6)
  - [ ] Link/clique no card navega para `/projetos/[id]`
- [ ] Indicador de tarefas atrasadas (AC: 7)
  - [ ] Query inclui contagem de tarefas com `prazo < hoje AND status != CONCLUIDA`
  - [ ] Se `tarefasAtrasadas > 0`: borda vermelha no card + badge contador

## Dev Notes
**Depende de:** Story 5.1 (models), Story 4.3 (projeto criado na conversão)

**Query de progresso:**
```ts
const projetos = await prisma.projeto.findMany({
  where: { status: 'ATIVO', organizacaoId },
  include: {
    lead: true,
    _count: { select: { tarefas: true } },
    tarefas: { select: { status: true, prazo: true } }
  }
})
// calcular progresso client-side ou na query
```

### Testing
- Verificar que apenas projetos ATIVOS aparecem
- Testar filtro por tipo de serviço
- Testar borda vermelha em projeto com tarefas atrasadas

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
