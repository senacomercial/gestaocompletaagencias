# Story 7.1: Schema — Squads, Agentes & Execuções

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["prisma:migrate", "typecheck"]

## Story
**As a** sistema,
**I want** os models de banco para squads, agentes e histórico de execuções,
**so that** o painel de orquestração de IA tenha estrutura de dados robusta.

## Acceptance Criteria
1. Model `Squad`: id, nome, descricao, avatar, cor, organizacaoId, ativo, createdAt
2. Model `Agente`: id, nome, role, icone, descricao, squadId (FK), organizacaoId, status enum (DISPONIVEL, EM_EXECUCAO, INATIVO, ERRO), configuracao (JSON), createdAt
3. Model `Execucao`: id, agenteId (FK), organizacaoId, comando, input (JSON nullable), output (Text nullable), status enum (PENDENTE, EM_ANDAMENTO, CONCLUIDA, FALHA), iniciadoEm, concluidoEm, duracaoMs, usuarioId (FK)
4. Model `LogExecucao`: id, execucaoId (FK), nivel enum (INFO, WARN, ERROR, SUCCESS), mensagem, timestamp
5. API routes CRUD: `/api/squads`, `/api/agentes`, `/api/execucoes`
6. Seed com squad "Core Agency" e agentes padrão
7. Variáveis `AIOS_API_URL` e `AIOS_API_KEY` documentadas

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar model Squad (AC: 1)
  - [ ] Campos: id, nome, descricao, avatar (url/emoji), cor (hex), organizacaoId, ativo (bool), createdAt
  - [ ] Index em organizacaoId
- [ ] Criar model Agente (AC: 2)
  - [ ] Enum `StatusAgente`: DISPONIVEL, EM_EXECUCAO, INATIVO, ERRO
  - [ ] Campos completos conforme AC
  - [ ] `configuracao Json?` — modelo, temperatura, prompts, etc.
  - [ ] FK para Squad (nullable — agente pode não ter squad)
- [ ] Criar model Execucao (AC: 3)
  - [ ] Enum `StatusExecucao`: PENDENTE, EM_ANDAMENTO, CONCLUIDA, FALHA
  - [ ] Campos: iniciadoEm, concluidoEm (nullable), duracaoMs (calculado)
  - [ ] FK para Agente e Usuario
- [ ] Criar model LogExecucao (AC: 4)
  - [ ] Enum `NivelLog`: INFO, WARN, ERROR, SUCCESS
  - [ ] Index em execucaoId para queries rápidas
- [ ] Aplicar migration (AC: 5)
  - [ ] `prisma migrate dev --name squad-agentes`
- [ ] Criar API routes (AC: 5)
  - [ ] `/api/squads` — GET + POST
  - [ ] `/api/squads/[id]` — GET + PUT + DELETE
  - [ ] `/api/agentes` — GET + POST
  - [ ] `/api/agentes/[id]` — GET + PUT + DELETE + PATCH status
  - [ ] `/api/execucoes` — GET + POST (criar execução)
  - [ ] `/api/execucoes/[id]/logs` — GET logs em tempo real
- [ ] Seed squad "Core Agency" (AC: 6)
  - [ ] Squad "Core Agency" com cor gold
  - [ ] Agentes: "Vendedor IA", "Cobrador IA", "Produtor IA", "Entregador IA"
- [ ] Documentar variáveis (AC: 7)
  - [ ] `AIOS_API_URL` e `AIOS_API_KEY` no `.env.example`

## Dev Notes
**Schema resumido:**
```prisma
enum StatusAgente { DISPONIVEL EM_EXECUCAO INATIVO ERRO }
enum StatusExecucao { PENDENTE EM_ANDAMENTO CONCLUIDA FALHA }
enum NivelLog { INFO WARN ERROR SUCCESS }

model Squad {
  id            String   @id @default(cuid())
  nome          String
  descricao     String?
  avatar        String?
  cor           String   @default("#D4AF37")
  organizacaoId String
  ativo         Boolean  @default(true)
  agentes       Agente[]
  createdAt     DateTime @default(now())
  @@index([organizacaoId])
}
```

**configuracao JSON exemplo:**
```json
{ "modelo": "claude-sonnet-4-6", "temperatura": 0.7, "maxTokens": 4096, "promptSistema": "..." }
```

### Testing
- Verificar migration aplica sem conflitos
- Testar CRUD completo via API
- Verificar seed cria squad e agentes corretamente

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
