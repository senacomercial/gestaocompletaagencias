# Story 5.3: Presets de Tarefas por Tipo de Serviço

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** gestor,
**I want** criar presets de tarefas para cada serviço,
**so that** eu aplique um conjunto padrão de tarefas com um clique em novos projetos.

## Acceptance Criteria
1. Página `/configuracoes/presets` com CRUD completo de presets
2. Cada preset tem nome e lista de tarefas com drag-and-drop para reordenar
3. Botão "Aplicar Preset" no projeto abre modal com seleção de preset
4. Aplicar preset: tarefas criadas em bulk, ordem preservada
5. Opção de aplicar parcialmente (checkboxes por tarefa)
6. Duplicar preset existente para criar variações

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar página de presets (AC: 1)
  - [ ] `src/app/(dashboard)/configuracoes/presets/page.tsx`
  - [ ] Lista de presets com nome, contagem de tarefas, ações (editar, duplicar, excluir)
  - [ ] Botão "Novo Preset"
- [ ] Editor de preset (AC: 2)
  - [ ] Criar `src/components/presets/PresetEditor.tsx`
  - [ ] Input de nome do preset
  - [ ] Lista de tarefas do preset com drag-and-drop (dnd-kit)
  - [ ] Cada linha: handle, título, descrição (opcional), botão remover
  - [ ] Botão "Adicionar Tarefa" ao final
- [ ] Modal "Aplicar Preset" (AC: 3)
  - [ ] Botão na página `/projetos/[id]`
  - [ ] Criar `src/components/projetos/AplicarPresetModal.tsx`
  - [ ] Lista de presets disponíveis com radio selection
  - [ ] Preview das tarefas do preset selecionado
- [ ] Aplicação em bulk (AC: 4)
  - [ ] `POST /api/projetos/[id]/aplicar-preset`
  - [ ] Body: `{ presetId, tarefas: string[] }` (IDs das tarefas a criar)
  - [ ] `prisma.tarefa.createMany` com projetoId e ordem preservada
- [ ] Checkboxes para aplicação parcial (AC: 5)
  - [ ] Na modal: checkboxes ao lado de cada tarefa do preset
  - [ ] "Selecionar Todas" toggle
  - [ ] Apenas tarefas marcadas são criadas
- [ ] Duplicar preset (AC: 6)
  - [ ] Botão "Duplicar" em cada preset da listagem
  - [ ] `POST /api/presets/[id]/duplicar`
  - [ ] Cria cópia com nome "[Nome] (cópia)"

## Dev Notes
**Depende de:** Story 5.1 (models PresetServico, PresetTarefa)

**API aplicar preset:**
```ts
// POST /api/projetos/[id]/aplicar-preset
const presetTarefas = await prisma.presetTarefa.findMany({
  where: { presetServicoId: presetId },
  orderBy: { ordemPadrao: 'asc' }
})
await prisma.tarefa.createMany({
  data: tarefasSelecionadas.map((pt, idx) => ({
    titulo: pt.titulo, descricao: pt.descricao,
    projetoId, organizacaoId, ordem: idx,
    status: 'PENDENTE', responsavelId: session.user.id
  }))
})
```

### Testing
- Testar criação de preset com tarefas
- Testar aplicar preset cria tarefas com ordem correta
- Testar duplicar preset cria cópia independente

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
