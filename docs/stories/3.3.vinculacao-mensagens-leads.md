# Story 3.3: Vinculação de Mensagens a Leads

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** sistema,
**I want** identificar automaticamente qual lead corresponde a uma mensagem recebida,
**so that** o histórico fique organizado no perfil correto.

## Acceptance Criteria
1. Mensagem recebida → busca lead por telefone na mesma organização
2. Lead encontrado: mensagem salva com `leadId` preenchido
3. Lead não encontrado: mensagem salva com `leadId = null` → aparece em `/whatsapp/nao-identificadas`
4. Página de não-identificadas com opções "Vincular a Lead" ou "Criar novo Lead"
5. Ao vincular: mensagens anteriores associadas retroativamente
6. Toast de notificação com nome do lead ao receber mensagem
7. Badge de não lidas no card do lead no Kanban

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Lógica de vinculação automática no servidor (AC: 1, 2, 3)
  - [ ] Em `server/socket-server.js`: ao receber `messages.upsert`
  - [ ] Normalizar número: remover `@s.whatsapp.net`, `+`, espaços
  - [ ] Query: `prisma.lead.findFirst({ where: { telefone: normalized, organizacaoId } })`
  - [ ] Salvar mensagem com `leadId` (encontrado) ou `null`
- [ ] Adicionar campos ao model Lead (AC: 7)
  - [ ] `mensagensNaoLidas Int @default(0)` no Lead
  - [ ] Incrementar ao salvar mensagem recebida com leadId
  - [ ] Zerar ao abrir chat (Story 3.5)
- [ ] Criar página de não-identificadas (AC: 3, 4)
  - [ ] `src/app/(dashboard)/whatsapp/nao-identificadas/page.tsx`
  - [ ] Listar mensagens com `leadId = null`
  - [ ] Para cada grupo de número: mostrar histórico e opções
- [ ] Opção "Vincular a Lead" (AC: 4, 5)
  - [ ] Busca de leads por nome/empresa
  - [ ] `POST /api/mensagens/vincular`: atualizar todas as mensagens do número com `leadId`
  - [ ] Remover da lista de não-identificadas
- [ ] Opção "Criar novo Lead" (AC: 4)
  - [ ] Abrir modal de criação com telefone pré-preenchido
  - [ ] Após criar: vincular mensagens automaticamente
- [ ] Toast de notificação (AC: 6)
  - [ ] Evento Socket.io `message:new` recebido no cliente
  - [ ] Toast: avatar do lead + nome + preview da mensagem
  - [ ] Clique no toast abre slide-over do lead

## Dev Notes
**Normalização de número:**
```js
function normalizePhone(phone) {
  return phone.replace(/\D/g, '').replace(/^55/, '')
}
// Testar variações: +55 11 99999-9999, 5511999999999, 11999999999
```

**Depende de:** Story 3.1 (servidor Baileys), Story 2.3 (Kanban para badge), Story 2.4 (slide-over)

**API route vinculação:**
```ts
// POST /api/mensagens/vincular
// Body: { telefone: string, leadId: string }
// Atualiza todas mensagens do telefone com leadId
```

### Testing
- Testar matching de telefone com e sem código do país
- Testar vinculação retroativa de mensagens
- Verificar badge de não lidas incrementa corretamente

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
