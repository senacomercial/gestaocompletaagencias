# Story 4.1: Schema — Modelos de Conversão, Projeto e Financeiro

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["prisma:migrate", "typecheck"]

## Story
**As a** sistema,
**I want** os models de banco para conversão de leads, projetos e contratos financeiros,
**so that** o pipeline de conversão automática tenha estrutura de dados sólida.

## Acceptance Criteria
1. Campos adicionados ao `Lead`: `status` enum (ATIVO, VENDA_REALIZADA, PERDIDO), `vgvTotal` (Decimal), `recorrenciaMensal` (Decimal nullable), `dataConversao` (DateTime nullable)
2. Model `Projeto`: id, nome, leadId (FK), organizacaoId, tipoServico, status enum (ATIVO, PAUSADO, CONCLUIDO, CANCELADO), createdAt, updatedAt
3. Model `ContratoFinanceiro`: id, projetoId (FK), leadId (FK), organizacaoId, valorTotal, recorrenciaMensal, tipoPagamento enum (RECORRENTE, PARCELADO, AVULSO), dataInicio, numeroParcelas, createdAt
4. Model `Lancamento`: id, contratoId (FK), organizacaoId, descricao, valor, dataVencimento, status enum (PENDENTE, PAGO, ATRASADO), tipo enum (RECORRENTE, UNICO), createdAt
5. Migrations aplicadas; seed com exemplo completo de conversão

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Atualizar model Lead (AC: 1)
  - [ ] Adicionar enum `StatusLead`: ATIVO, VENDA_REALIZADA, PERDIDO
  - [ ] Campos: `status StatusLead @default(ATIVO)`, `vgvTotal Decimal?`, `recorrenciaMensal Decimal?`, `dataConversao DateTime?`
- [ ] Criar model Projeto (AC: 2)
  - [ ] Enum `StatusProjeto`: ATIVO, PAUSADO, CONCLUIDO, CANCELADO
  - [ ] Model `Projeto` com todos os campos listados
  - [ ] FK para Lead, índices em organizacaoId
- [ ] Criar model ContratoFinanceiro (AC: 3)
  - [ ] Enum `TipoPagamento`: RECORRENTE, PARCELADO, AVULSO
  - [ ] Model `ContratoFinanceiro` com todos os campos
  - [ ] FK para Projeto e Lead
- [ ] Criar model Lancamento (AC: 4)
  - [ ] Enum `StatusLancamento`: PENDENTE, PAGO, ATRASADO
  - [ ] Enum `TipoLancamento`: RECORRENTE, UNICO
  - [ ] Model `Lancamento` com todos os campos
  - [ ] FK para ContratoFinanceiro
- [ ] Aplicar migration (AC: 5)
  - [ ] `prisma migrate dev --name conversao-financeiro`
- [ ] Atualizar seed com exemplo (AC: 5)
  - [ ] Criar lead convertido → projeto → contrato → lançamentos de exemplo

## Dev Notes
**Schema resumido:**
```prisma
enum StatusLead { ATIVO VENDA_REALIZADA PERDIDO }
enum StatusProjeto { ATIVO PAUSADO CONCLUIDO CANCELADO }
enum TipoPagamento { RECORRENTE PARCELADO AVULSO }
enum StatusLancamento { PENDENTE PAGO ATRASADO }
enum TipoLancamento { RECORRENTE UNICO }

model Lead {
  // ... campos existentes ...
  status            StatusLead         @default(ATIVO)
  vgvTotal          Decimal?
  recorrenciaMensal Decimal?
  dataConversao     DateTime?
  projetos          Projeto[]
  contratos         ContratoFinanceiro[]
}
```

**Nota:** `Decimal` do Prisma mapeia para `DECIMAL(10,2)` no PostgreSQL — adequado para valores monetários.

### Testing
- Verificar migration aplica sem conflitos
- Verificar seed cria o exemplo completo
- Verificar tipos TypeScript gerados pelo Prisma

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
