# Story 2.4: CRUD de Leads & Slide-over de Detalhes

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** vendedor,
**I want** criar leads e visualizar/editar seus detalhes em um painel lateral,
**so that** eu registre e gerencie contatos sem perder o contexto do Kanban.

## Acceptance Criteria
1. Botão "Novo Lead" abre modal com campos: nome, email, telefone, empresa, funil, etapa inicial
2. Lead criado aparece imediatamente no Kanban
3. Clicar em card abre slide-over à direita com todos os dados
4. Slide-over: dados editáveis inline, etapa atual, tags, histórico de movimentações
5. Save automático (debounce 800ms) ou botão "Salvar"
6. Botão "Excluir Lead" com confirmação
7. Slide-over fecha com ESC ou clique fora
8. Responsivo: slide-over 100% em mobile

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar modal de novo lead (AC: 1)
  - [ ] Criar `src/components/crm/CreateLeadModal.tsx`
  - [ ] Formulário: nome (obrigatório), email, telefone, empresa, funil, etapa
  - [ ] Campos de funil/etapa: selects com dados da API
  - [ ] Validação Zod no client
- [ ] Atualização imediata no Kanban (AC: 2)
  - [ ] Após POST /api/leads, adicionar card à coluna correspondente
  - [ ] Optimistic insert ou revalidar query
- [ ] Criar componente LeadSlideOver (AC: 3)
  - [ ] Criar `src/components/crm/LeadSlideOver.tsx`
  - [ ] Usar `Sheet` do shadcn/ui (slide da direita)
  - [ ] Carregar dados do lead ao abrir
- [ ] Dados editáveis inline (AC: 4, 5)
  - [ ] Campos: nome, email, telefone, empresa (todos editáveis in-place)
  - [ ] Select de etapa atual (permite mover lead sem drag)
  - [ ] Campo de tags (Story 2.5)
  - [ ] Histórico de movimentações (timestamp + de/para etapa)
  - [ ] Auto-save com debounce de 800ms após qualquer mudança
  - [ ] Indicador visual "Salvando..." / "Salvo ✓"
- [ ] Botão excluir (AC: 6)
  - [ ] Botão vermelho na parte inferior do slide-over
  - [ ] Diálogo de confirmação
  - [ ] Após excluir: fechar slide-over, remover card do Kanban
- [ ] Fechar com ESC e click fora (AC: 7)
  - [ ] `Sheet` do shadcn/ui já suporta isso nativamente
- [ ] Responsividade (AC: 8)
  - [ ] Em mobile: slide-over ocupa 100% da largura
  - [ ] Em desktop: 480px de largura

## Dev Notes
**Componente Sheet do shadcn/ui:**
```tsx
import { Sheet, SheetContent } from '@/components/ui/sheet'

<Sheet open={isOpen} onOpenChange={setIsOpen}>
  <SheetContent side="right" className="w-full sm:w-[480px]">
    {/* conteúdo */}
  </SheetContent>
</Sheet>
```

**Auto-save pattern:**
```ts
import { useDebouncedCallback } from 'use-debounce'
const saveField = useDebouncedCallback(async (field, value) => {
  await fetch(`/api/leads/${leadId}`, {
    method: 'PUT',
    body: JSON.stringify({ [field]: value })
  })
}, 800)
```

**Depende de:** Story 2.1 (API), Story 2.3 (Kanban)

### Testing
- Testar criação de lead aparece no Kanban sem reload
- Testar auto-save debounced persiste corretamente
- Testar exclusão remove da lista

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
