# Story 3.1: Servidor Socket.io + Baileys (Backend)

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** sistema,
**I want** um servidor Node.js dedicado com Baileys e Socket.io,
**so that** a conexão WhatsApp seja persistente e eventos de mensagem sejam transmitidos em tempo real.

## Acceptance Criteria
1. Arquivo `server/socket-server.js` com servidor Socket.io standalone
2. Baileys integrado: inicializa sessão WhatsApp por `organizacaoId`
3. Auth state persistido em `server/sessions/{organizacaoId}/`
4. Eventos Socket.io: `qr`, `connected`, `disconnected`, `message:new`
5. Model `Mensagem` criado: id, corpo, fromMe, timestamp, whatsappId, telefoneContato, leadId (nullable), organizacaoId
6. `npm run socket` inicia o servidor corretamente
7. Variáveis `SOCKET_PORT`, `NEXT_PUBLIC_SOCKET_URL` documentadas

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar servidor Socket.io standalone (AC: 1)
  - [ ] `server/socket-server.js` com Express + Socket.io
  - [ ] CORS configurado para aceitar `NEXT_PUBLIC_APP_URL`
  - [ ] Porta configurável via `SOCKET_PORT` (padrão: 3001)
- [ ] Integrar Baileys (AC: 2)
  - [ ] `npm install @whiskeysockets/baileys`
  - [ ] Função `createWhatsAppSession(organizacaoId)`
  - [ ] Multi-session: Map de conexões por organizacaoId
- [ ] Persistência de auth state (AC: 3)
  - [ ] `useMultiFileAuthState('server/sessions/${organizacaoId}')`
  - [ ] Criar pasta `server/sessions/` (gitignore)
  - [ ] Reconexão automática se sessão existir
- [ ] Implementar eventos Socket.io (AC: 4)
  - [ ] `qr`: emitir QR code como base64 ao cliente
  - [ ] `connected`: emitir número e nome do perfil
  - [ ] `disconnected`: emitir motivo da desconexão
  - [ ] `message:new`: emitir mensagem recebida em tempo real
- [ ] Model `Mensagem` no Prisma (AC: 5)
  - [ ] Adicionar ao schema: id, corpo, fromMe, timestamp, whatsappId, telefoneContato, leadId (nullable, FK), organizacaoId
  - [ ] Migration: `prisma migrate dev --name whatsapp-mensagem`
  - [ ] Salvar mensagens recebidas no banco via Prisma (usar REST API ou conexão direta)
- [ ] Script npm (AC: 6)
  - [ ] Adicionar `"socket": "node server/socket-server.js"` no package.json
- [ ] Documentar variáveis (AC: 7)
  - [ ] Adicionar `SOCKET_PORT` e `NEXT_PUBLIC_SOCKET_URL` ao `.env.example`

## Dev Notes
**Atenção:** O servidor socket é um processo Node.js SEPARADO do Next.js. Roda na porta 3001 (ou `SOCKET_PORT`).

**Baileys pattern:**
```js
const { default: makeWASocket, useMultiFileAuthState } = require('@whiskeysockets/baileys')

async function createSession(orgId, socket) {
  const { state, saveCreds } = await useMultiFileAuthState(`server/sessions/${orgId}`)
  const sock = makeWASocket({ auth: state, printQRInTerminal: false })

  sock.ev.on('connection.update', ({ qr, connection }) => {
    if (qr) socket.emit('qr', qr)
    if (connection === 'open') socket.emit('connected', { number: sock.user.id })
    if (connection === 'close') socket.emit('disconnected')
  })

  sock.ev.on('creds.update', saveCreds)
  sock.ev.on('messages.upsert', ({ messages }) => {
    messages.forEach(msg => socket.to(orgId).emit('message:new', msg))
  })
}
```

**Comunicação com banco:** O servidor socket pode chamar as API routes do Next.js via HTTP local, ou importar o Prisma Client diretamente.

### Testing
- Testar `npm run socket` inicia sem erros
- Testar eventos QR são emitidos corretamente
- Verificar sessão persiste após restart

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
