# Story 1.1: Scaffold do Projeto

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** desenvolvedor,
**I want** o projeto Next.js configurado com todas as dependências base,
**so that** eu possa iniciar o desenvolvimento dos módulos sem atrito de configuração.

## Acceptance Criteria
1. Projeto Next.js 14 (App Router) com TypeScript criado e rodando em `localhost:3000`
2. Tailwind CSS configurado com tema `agency-gold` (tokens de cor: gold, bronze, dark surfaces) em `tailwind.config.ts`
3. shadcn/ui instalado e inicializado com tema dark customizado (Agency Gold como accent)
4. Aliases de path configurados (`@/*` → `src/*`)
5. ESLint + Prettier configurados com regras padrão
6. Estrutura de pastas criada: `src/app/`, `src/components/`, `src/lib/`, `src/types/`, `src/hooks/`
7. Variáveis de ambiente documentadas em `.env.example`

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`. Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Verificar estrutura existente do projeto Next.js (AC: 1)
  - [ ] Confirmar versão Next.js 14 App Router
  - [ ] Confirmar TypeScript configurado
- [ ] Configurar Tailwind com tema Agency Gold (AC: 2)
  - [ ] Adicionar tokens de cor: `agency-gold: #D4AF37`, `agency-bronze: #CD7F32`
  - [ ] Adicionar tokens de superfícies dark: `#0A0A0A`, `#111111`, `#1A1A1A`, `#222222`
  - [ ] Adicionar tokens de texto: `#F5F5F5`, `#A1A1A1`, `#525252`
- [ ] Instalar e configurar shadcn/ui (AC: 3)
  - [ ] `npx shadcn-ui@latest init` com tema dark
  - [ ] Definir accent como Agency Gold `#D4AF37`
- [ ] Configurar path alias (AC: 4)
  - [ ] Verificar/ajustar `tsconfig.json` com `"@/*": ["./src/*"]`
  - [ ] Verificar `next.config.js` se necessário
- [ ] Configurar ESLint + Prettier (AC: 5)
  - [ ] Instalar dependências: `eslint-config-prettier`, `prettier`
  - [ ] Criar `.prettierrc` com regras padrão
  - [ ] Verificar `.eslintrc.json`
- [ ] Criar estrutura de pastas (AC: 6)
  - [ ] `src/app/`, `src/components/`, `src/lib/`, `src/types/`, `src/hooks/`
- [ ] Documentar variáveis de ambiente (AC: 7)
  - [ ] Criar/atualizar `.env.example` com todas as vars necessárias

## Dev Notes
**Stack:** Next.js 14 (App Router), TypeScript 5.x, Tailwind CSS 3.x, shadcn/ui

**Paleta Agency Gold:**
```
Accent:        #D4AF37  (gold)
Accent Hover:  #F5CC50
Bronze:        #CD7F32
Fundo Base:    #0A0A0A
Superfície:    #111111
Card:          #1A1A1A
Hover:         #222222
Borda:         #2A2A2A
Texto:         #F5F5F5 / #A1A1A1 / #525252
```

**Variáveis de ambiente necessárias:**
- `DATABASE_URL` — PostgreSQL connection string
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`
- `NEXT_PUBLIC_APP_URL`
- `NEXT_PUBLIC_SOCKET_URL`
- `AIOS_API_URL`
- `AIOS_API_KEY`

### Testing
- Verificar `npm run build` sem erros
- Verificar `npm run lint` sem warnings
- Verificar `npm run typecheck` sem erros de tipo

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
