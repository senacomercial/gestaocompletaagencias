# Story 7.5: Integração AIOS — API & Webhook para Agentes Externos

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** sistema,
**I want** endpoints de API e webhooks para comunicação com agentes AIOS externos,
**so that** squads de IA externos possam integrar com a plataforma em tempo real.

## Acceptance Criteria
1. `POST /api/aios/webhook`: recebe eventos autenticados via `AIOS_API_KEY`
2. Eventos suportados: `execucao:iniciada`, `execucao:log`, `execucao:concluida`, `execucao:falha`
3. Webhook atualiza banco e emite evento Socket.io em tempo real
4. `GET /api/aios/agentes`: lista agentes com status atual
5. `POST /api/aios/execucoes`: agente externo registra execução e recebe `execucaoId`
6. Painel `/squad/integracoes`: URL do webhook, API Key (mascarada), instruções com curl de exemplo
7. Log de webhooks dos últimos 7 dias com status de processamento

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Middleware de autenticação AIOS (AC: 1)
  - [ ] Criar `src/lib/aios-auth.ts`
  - [ ] Verificar header `Authorization: Bearer ${AIOS_API_KEY}`
  - [ ] Retornar 401 se inválido
- [ ] Webhook endpoint (AC: 1, 2, 3)
  - [ ] `src/app/api/aios/webhook/route.ts`
  - [ ] Validar `AIOS_API_KEY` via middleware
  - [ ] Parse do evento: `{ tipo, execucaoId, organizacaoId, payload }`
  - [ ] Handler por tipo de evento:
    - `execucao:iniciada` → status EM_ANDAMENTO, iniciadoEm
    - `execucao:log` → criar LogExecucao
    - `execucao:concluida` → status CONCLUIDA, output, concluidoEm, duracaoMs
    - `execucao:falha` → status FALHA, output (erro)
  - [ ] Emitir Socket.io para a organização após cada update
- [ ] API de agentes para AIOS (AC: 4)
  - [ ] `src/app/api/aios/agentes/route.ts`
  - [ ] Requer `AIOS_API_KEY`
  - [ ] Retorna lista de agentes com status, configuração
- [ ] API de criação de execução (AC: 5)
  - [ ] `src/app/api/aios/execucoes/route.ts` — POST
  - [ ] Cria Execucao e retorna `{ execucaoId }`
  - [ ] Agente externo usa este ID para reportar progresso
- [ ] Painel de integrações (AC: 6)
  - [ ] `src/app/(dashboard)/squad/integracoes/page.tsx`
  - [ ] Exibir URL do webhook (NEXT_PUBLIC_APP_URL + /api/aios/webhook)
  - [ ] API Key mascarada: primeiros 8 chars + "•••••"
  - [ ] Botão "Revelar" com confirmação
  - [ ] Exemplos curl para cada tipo de evento
- [ ] Log de webhooks (AC: 7)
  - [ ] Model `WebhookLog`: id, tipo, status (SUCCESS/ERROR), payload (JSON), responseTime, organizacaoId, createdAt
  - [ ] Tabela na página de integrações com últimos 7 dias

## Dev Notes
**Depende de:** Story 7.1 (models), Story 7.4 (fluxo de execução)

**Validação AIOS_API_KEY:**
```ts
function validateAiosAuth(req: Request): boolean {
  const auth = req.headers.get('authorization')
  return auth === `Bearer ${process.env.AIOS_API_KEY}`
}
```

**Estrutura do webhook payload:**
```json
{
  "tipo": "execucao:log",
  "execucaoId": "clx...",
  "organizacaoId": "clx...",
  "payload": {
    "nivel": "INFO",
    "mensagem": "Processando tarefa..."
  }
}
```

### Testing
- Testar webhook com API_KEY inválida retorna 401
- Testar cada tipo de evento atualiza banco corretamente
- Testar webhook log registra todas as requisições

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
