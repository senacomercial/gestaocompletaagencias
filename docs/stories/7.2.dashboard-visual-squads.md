# Story 7.2: Dashboard Visual de Squads

## Status
Draft

## Executor Assignment
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** gestor,
**I want** visualizar todos os squads e agentes em um painel centralizado,
**so that** eu tenha visão completa do status operacional das equipes de IA.

## Acceptance Criteria
1. Página `/squad` com grid de cards de squads
2. Card: avatar, nome, descrição, contagem de agentes, status geral
3. Expandir card: lista de agentes com ícone, nome, status badge, última execução
4. Status atualiza via Socket.io (polling 15s como fallback)
5. Métricas: Total de Agentes Ativos, Execuções Hoje, Taxa de Sucesso, Tempo Médio
6. Agentes com ERRO com borda vermelha pulsante
7. Filtro por status
8. Botões "+ Novo Squad" e "+ Novo Agente"

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar página /squad (AC: 1)
  - [ ] `src/app/(dashboard)/squad/page.tsx`
  - [ ] Grid de cards de squads
  - [ ] API `GET /api/squads` com agentes inclusos
- [ ] Componente SquadCard (AC: 2, 3)
  - [ ] Criar `src/components/squad/SquadCard.tsx`
  - [ ] Header: avatar/emoji, nome, descrição, cor do squad
  - [ ] Contador de agentes por status
  - [ ] Expandível: lista de agentes ao clicar
  - [ ] AgenteRow: ícone, nome, badge status, última execução timestamp
- [ ] Atualização em tempo real (AC: 4)
  - [ ] Socket.io: ouvir `agente:status-update` com `{ agenteId, status }`
  - [ ] Polling fallback: `setInterval(fetchStatus, 15000)`
  - [ ] Atualizar badge de status sem re-render completo
- [ ] Métricas do squad (AC: 5)
  - [ ] Criar `src/components/squad/SquadMetrics.tsx`
  - [ ] `GET /api/squad/metricas` com dados agregados
  - [ ] 4 KPI cards: Agentes Ativos, Execuções Hoje, Taxa Sucesso, Tempo Médio
  - [ ] Taxa de sucesso: cor gold ≥ 90%, âmbar 70-89%, vermelho < 70%
- [ ] Indicador de ERRO (AC: 6)
  - [ ] Agente com `status === 'ERRO'`: borda vermelha pulsante (CSS animation)
  - [ ] `@keyframes pulse` com box-shadow vermelho
- [ ] Filtro por status (AC: 7)
  - [ ] Buttons toggle: Todos | Disponível | Em Execução | Erro | Inativo
  - [ ] Filtrar agentes dentro dos cards
- [ ] Botões de criação (AC: 8)
  - [ ] "+ Novo Squad": abre modal com nome, cor, avatar
  - [ ] "+ Novo Agente": abre modal com squad (select), nome, role, configuração

## Dev Notes
**Depende de:** Story 7.1 (models e API)

**CSS pulsante para erro:**
```css
@keyframes errorPulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4); }
  50% { box-shadow: 0 0 0 6px rgba(239, 68, 68, 0); }
}
.agente-erro { animation: errorPulse 2s infinite; border: 1px solid #EF4444; }
```

**Socket.io emit do servidor (ao mudar status do agente):**
```js
io.to(organizacaoId).emit('agente:status-update', { agenteId, status })
```

### Testing
- Testar polling de 15s atualiza status
- Testar borda pulsante aparece em agente com ERRO
- Testar filtro de status funciona corretamente

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
