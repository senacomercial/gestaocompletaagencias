# Story 2.3: Visualização Kanban com Drag-and-Drop

## Status
Draft

## Executor Assignment
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** usuário,
**I want** visualizar meus leads em formato Kanban organizado por etapas,
**so that** eu tenha visão clara do pipeline e mova leads entre etapas com facilidade.

## Acceptance Criteria
1. Página `/crm` exibe Kanban com colunas por etapas do funil ativo
2. Seletor de funil no topo para alternar entre funis
3. Cada coluna exibe: nome da etapa, cor, contador de leads e soma de VGV
4. Cards de lead: nome, empresa, telefone, tags (badges), avatar com iniciais
5. Drag-and-drop funcional entre colunas (dnd-kit) com chamada à API
6. Optimistic update com rollback em caso de erro
7. Colunas com scroll vertical independente
8. Design dark: colunas `#1A1A1A`, cards `#222222`

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar página CRM/Kanban (AC: 1)
  - [ ] `src/app/(dashboard)/crm/page.tsx`
  - [ ] Layout horizontal com overflow-x scroll
  - [ ] Carregar etapas do funil ativo e leads
- [ ] Seletor de funil ativo (AC: 2)
  - [ ] Dropdown no header da página
  - [ ] Trocar funil recarrega colunas
- [ ] Criar componente KanbanColumn (AC: 3)
  - [ ] Criar `src/components/crm/KanbanColumn.tsx`
  - [ ] Header: cor da etapa (badge/dot), nome, contador de leads, soma VGV
  - [ ] Drop zone para receber leads
- [ ] Criar componente LeadCard (AC: 4)
  - [ ] Criar `src/components/crm/LeadCard.tsx`
  - [ ] Avatar: iniciais do nome em circle, gradiente gold→bronze
  - [ ] Nome, empresa (muted), telefone
  - [ ] Tags como badges coloridos (max 3 visíveis + "+N")
  - [ ] Handle de drag
- [ ] Implementar DnD entre colunas (AC: 5)
  - [ ] `DndContext` englobando todo o Kanban
  - [ ] Cada coluna é um `DroppableContainer`
  - [ ] Cada card é `Draggable`
  - [ ] `onDragEnd`: chamar `PATCH /api/leads/[id]/etapa`
- [ ] Implementar optimistic update (AC: 6)
  - [ ] Mover card visualmente antes da resposta da API
  - [ ] Em caso de erro: reverter posição + toast de erro
- [ ] Scroll independente por coluna (AC: 7)
  - [ ] Cada coluna com `overflow-y: auto`, `max-height: calc(100vh - 200px)`
- [ ] Aplicar design dark (AC: 8)
  - [ ] Fundo coluna: `#1A1A1A`
  - [ ] Fundo card: `#222222`
  - [ ] Hover card: `#2A2A2A`

## Dev Notes
**Depende de:** Story 2.1 (API), Story 2.2 (seletor de funil)

**Estrutura DnD:**
```tsx
<DndContext sensors={sensors} onDragEnd={handleDragEnd}>
  <div className="flex gap-4 overflow-x-auto">
    {etapas.map(etapa => (
      <KanbanColumn key={etapa.id} etapa={etapa} leads={leadsByEtapa[etapa.id]} />
    ))}
  </div>
</DndContext>
```

**Largura de coluna:** 280px fixo, colunas não diminuem

**Performance:** Limitar a 100 leads por coluna com paginação virtual se necessário (react-window)

### Testing
- Testar drag entre colunas atualiza etapaId no banco
- Testar rollback: simular erro 500 e verificar que card volta à posição original
- Verificar scroll independente de colunas

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
