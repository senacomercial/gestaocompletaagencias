# Story 7.3: Painel de Agente Individual

## Status
Draft

## Executor Assignment
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** gestor,
**I want** abrir o perfil de um agente e visualizar configurações, histórico e logs,
**so that** eu monitore e configure cada agente individualmente.

## Acceptance Criteria
1. Slide-over com: nome, role, squad, status atual, configurações (JSON editável)
2. Aba "Histórico": últimas 20 execuções — comando, status, duração, usuário, timestamp
3. Aba "Logs em Tempo Real": stream via Socket.io com cores por nível
4. Aba "Configurações": modelo de IA, temperatura, max tokens, prompt de sistema
5. Botão "Forçar Disponível" com confirmação
6. Botão "Desativar Agente"
7. Gráfico mini "Execuções por Dia" (7 dias)
8. Taxa de sucesso com cor: gold ≥ 90%, âmbar 70-89%, vermelho < 70%

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar AgenteSlideOver (AC: 1)
  - [ ] Criar `src/components/squad/AgenteSlideOver.tsx`
  - [ ] Sheet do shadcn/ui com width 560px
  - [ ] Header: ícone, nome, role badge, squad badge, status badge
  - [ ] Tabs: Histórico | Logs | Configurações
- [ ] Aba Histórico (AC: 2)
  - [ ] `GET /api/agentes/[id]/execucoes?limit=20`
  - [ ] Tabela: comando (truncado), status (badge colorido), duração (ms→"2.3s"), usuário, timestamp relativo
  - [ ] Status: CONCLUIDA=verde, FALHA=vermelho, EM_ANDAMENTO=âmbar
- [ ] Aba Logs em Tempo Real (AC: 3)
  - [ ] Ouvir `execucao:log` do Socket.io filtrado por `agenteId`
  - [ ] Terminal-style: fundo `#0A0A0A`, monospace
  - [ ] Cores: INFO=branco, WARN=âmbar, ERROR=vermelho, SUCCESS=verde
  - [ ] Auto-scroll com overflow-y scroll
  - [ ] Botão "Limpar"
- [ ] Aba Configurações (AC: 4)
  - [ ] Criar `src/components/squad/AgenteConfigForm.tsx`
  - [ ] Campos: modelo IA (select), temperatura (slider 0-1), max tokens (number), prompt de sistema (textarea)
  - [ ] Salvar via `PATCH /api/agentes/[id]`
  - [ ] Auto-save com debounce ou botão Salvar
- [ ] Botão Forçar Disponível (AC: 5)
  - [ ] Visível apenas se `status !== 'DISPONIVEL'`
  - [ ] Diálogo de confirmação
  - [ ] `PATCH /api/agentes/[id]/status` → `{ status: 'DISPONIVEL' }`
- [ ] Botão Desativar (AC: 6)
  - [ ] Diálogo de confirmação
  - [ ] `PATCH /api/agentes/[id]/status` → `{ status: 'INATIVO' }`
- [ ] Gráfico mini (AC: 7)
  - [ ] `BarChart` compacto (altura 60px) com 7 dias
  - [ ] Barras: verde (sucesso), vermelho (falha)
  - [ ] `GET /api/agentes/[id]/execucoes-por-dia`
- [ ] Taxa de sucesso (AC: 8)
  - [ ] Calcular: CONCLUIDA / (CONCLUIDA + FALHA) * 100
  - [ ] Badge com cor dinâmica

## Dev Notes
**Depende de:** Story 7.1 (models), Story 7.2 (dashboard que abre o slide-over)

**Socket.io room do agente:** `agente:{agenteId}` para streams de log.

**Slider de temperatura:**
```tsx
import { Slider } from '@/components/ui/slider'
<Slider min={0} max={1} step={0.1} value={[temperatura]} onValueChange={([v]) => setTemperatura(v)} />
```

### Testing
- Testar histórico carrega 20 execuções
- Testar logs em tempo real chegam pelo socket
- Testar forçar disponível atualiza status

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
