# Story 4.3: Automação — Criação de Projeto no Módulo Projetos

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** sistema,
**I want** criar automaticamente um projeto ativo ao confirmar uma venda,
**so that** o trabalho operacional seja iniciado imediatamente sem intervenção manual.

## Acceptance Criteria
1. API route `POST /api/conversao` executa conversão em transação Prisma atômica
2. `Lead.status` → VENDA_REALIZADA, `dataConversao` e `vgvTotal` preenchidos
3. `Projeto` criado: nome = `[Empresa] — [Tipo de Serviço]`, status = ATIVO
4. Projeto visível imediatamente em `/projetos`
5. Card do lead exibe badge "VENDA" em gradiente gold→bronze
6. Lead convertido não pode ser movido para outras etapas
7. Toast de sucesso: "🏆 Venda registrada! Projeto criado para [Cliente]"
8. Falha reverte transação completamente

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar API route de conversão (AC: 1)
  - [ ] `src/app/api/conversao/route.ts` — POST
  - [ ] Transação Prisma com `prisma.$transaction`
  - [ ] Validação Zod do body de entrada
- [ ] Atualizar Lead na transação (AC: 2)
  - [ ] `status: VENDA_REALIZADA`
  - [ ] `dataConversao: new Date()`
  - [ ] `vgvTotal`, `recorrenciaMensal` do modal
- [ ] Criar Projeto na transação (AC: 3)
  - [ ] Nome: `${lead.empresa} — ${tipoServico}` (ou nome do lead se sem empresa)
  - [ ] `status: ATIVO`, `organizacaoId`, `leadId`
- [ ] Verificar visibilidade em /projetos (AC: 4)
  - [ ] Revalidar cache após conversão
  - [ ] Projeto aparece na listagem sem reload manual
- [ ] Badge "VENDA" no card do Kanban (AC: 5)
  - [ ] LeadCard exibe badge se `lead.status === 'VENDA_REALIZADA'`
  - [ ] Badge: gradiente `#D4AF37 → #CD7F32`, texto "VENDA", position absolute top-right
- [ ] Bloquear movimentação de lead convertido (AC: 6)
  - [ ] `PATCH /api/leads/[id]/etapa` → verificar `lead.status !== 'VENDA_REALIZADA'`
  - [ ] Retornar 400 se tentar mover lead convertido
  - [ ] No DnD: desabilitar drag se status === VENDA_REALIZADA
- [ ] Toast de sucesso (AC: 7)
  - [ ] Usar `toast.success()` com mensagem formatada
  - [ ] Incluir nome do cliente
- [ ] Rollback em caso de erro (AC: 8)
  - [ ] `prisma.$transaction` garante atomicidade
  - [ ] Toast de erro amigável se falhar

## Dev Notes
**Transação Prisma:**
```ts
await prisma.$transaction(async (tx) => {
  const lead = await tx.lead.update({ where: { id: leadId }, data: { status: 'VENDA_REALIZADA', vgvTotal, dataConversao: new Date() } })
  const projeto = await tx.projeto.create({ data: { nome: `${lead.empresa} — ${tipoServico}`, leadId, organizacaoId, tipoServico, status: 'ATIVO' } })
  return { lead, projeto }
})
```

**Depende de:** Story 4.1 (models), Story 4.2 (modal — chama esta API)

### Testing
- Teste unitário: transação cria lead + projeto juntos
- Teste: falha em criar projeto reverte atualização do lead
- Teste: lead VENDA_REALIZADA retorna 400 ao tentar mover

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
