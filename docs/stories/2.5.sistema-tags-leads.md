# Story 2.5: Sistema de Tags em Leads

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** usuário,
**I want** criar e atribuir tags coloridas aos leads,
**so that** eu filtre e categorize o pipeline.

## Acceptance Criteria
1. Model `Tag` (id, nome, cor, organizacaoId) + `LeadTag` (leadId, tagId) criados e migrados
2. Campo de tags no slide-over com autocomplete e opção "Criar tag: X"
3. Tags com seletor de cor (8 opções)
4. Tags exibidas como badges no slide-over e nos cards do Kanban
5. Filtro por tag no topo do Kanban
6. Página de gerenciamento em `/configuracoes/tags` com CRUD completo

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Adicionar models ao schema (AC: 1)
  - [ ] Model `Tag`: id, nome, cor, organizacaoId, createdAt
  - [ ] Model `LeadTag`: leadId (FK), tagId (FK) — chave composta
  - [ ] Relações: Tag hasMany LeadTag, Lead hasMany LeadTag
  - [ ] `prisma migrate dev --name tags`
- [ ] API routes de tags (AC: 1)
  - [ ] `GET/POST /api/tags` — listar e criar
  - [ ] `DELETE /api/tags/[id]` — excluir
  - [ ] `POST /api/leads/[id]/tags` — adicionar tag ao lead
  - [ ] `DELETE /api/leads/[id]/tags/[tagId]` — remover tag do lead
- [ ] Componente TagInput no slide-over (AC: 2)
  - [ ] Criar `src/components/crm/TagInput.tsx`
  - [ ] Input com autocomplete: filtrar tags existentes ao digitar
  - [ ] Opção "Criar tag: [nome]" quando não há match
  - [ ] Ao criar: abrir mini-modal de seleção de cor
  - [ ] Tags selecionadas como chips removíveis (X)
- [ ] Seletor de cor (AC: 3)
  - [ ] 8 swatches de cor: `#D4AF37`, `#CD7F32`, `#3B82F6`, `#22C55E`, `#F59E0B`, `#EF4444`, `#8B5CF6`, `#EC4899`
- [ ] Exibição nos cards do Kanban (AC: 4)
  - [ ] Até 3 tags como badges no card
  - [ ] Se mais de 3: badge `+N` em muted
  - [ ] Cor de fundo do badge = cor da tag (com alpha 20%)
- [ ] Filtro por tag no Kanban (AC: 5)
  - [ ] Dropdown multi-select de tags no header do Kanban
  - [ ] Filtrar: mostrar apenas leads que possuem TODAS as tags selecionadas
  - [ ] Badge contador de filtros ativos
- [ ] Página de gerenciamento de tags (AC: 6)
  - [ ] `src/app/(dashboard)/configuracoes/tags/page.tsx`
  - [ ] Lista de tags com nome, cor, contagem de leads
  - [ ] Editar nome/cor inline
  - [ ] Excluir tag (com confirmação — remove de todos os leads)

## Dev Notes
**Schema:**
```prisma
model Tag {
  id            String    @id @default(cuid())
  nome          String
  cor           String    @default("#3B82F6")
  organizacaoId String
  leads         LeadTag[]
  @@index([organizacaoId])
}

model LeadTag {
  leadId String
  tagId  String
  lead   Lead @relation(fields: [leadId], references: [id], onDelete: Cascade)
  tag    Tag  @relation(fields: [tagId], references: [id], onDelete: Cascade)
  @@id([leadId, tagId])
}
```

**Depende de:** Story 2.1 (Lead model), Story 2.4 (slide-over)

### Testing
- Testar criar tag nova via slide-over
- Testar filtro por tag no Kanban
- Testar exclusão de tag remove de todos os leads

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
