# Story 6.5: Alertas de Vencimento & Notificações Financeiras

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** gestor financeiro,
**I want** alertas automáticos sobre vencimentos e inadimplências,
**so that** eu tome ação antes de perder receita.

## Acceptance Criteria
1. Badge no ícone Financeiro: vencendo hoje + atrasados
2. Toast automático ao login se houver atrasados
3. Painel de notificações financeiras via ícone de sino
4. Badge "Vence Hoje" em âmbar nos lançamentos
5. Atualização automática de PENDENTE → ATRASADO quando vencimento passa
6. Filtro rápido "Ver Inadimplentes"
7. Exportação para CSV

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Badge no ícone Financeiro na sidebar (AC: 1)
  - [ ] `GET /api/financeiro/alertas-count`
  - [ ] Contar: atrasados + vencendo hoje
  - [ ] Badge vermelho no ícone na sidebar
  - [ ] Atualizar a cada 5 min (polling) ou via Socket.io
- [ ] Toast ao login com alertas (AC: 2)
  - [ ] Verificar atrasados após autenticação
  - [ ] Se `atrasados > 0`: toast de alerta com contagem
  - [ ] Botão "Ver detalhes" no toast → navega para `/financeiro`
  - [ ] Exibir apenas uma vez por sessão (localStorage flag)
- [ ] Painel de notificações (AC: 3)
  - [ ] Criar `src/components/notifications/NotificacoesPanel.tsx`
  - [ ] Ícone de sino no header com badge contador
  - [ ] Dropdown com lista de alertas financeiros recentes
  - [ ] Cada item: ícone, descrição, data, link de ação
- [ ] Badge "Vence Hoje" nos lançamentos (AC: 4)
  - [ ] Na lista de lançamentos: verificar `dataVencimento = hoje`
  - [ ] Badge âmbar: "Vence Hoje"
- [ ] Cron job PENDENTE → ATRASADO (AC: 5)
  - [ ] Criar `src/app/api/cron/atualizar-status/route.ts`
  - [ ] Configurar cron no Vercel (ou usar endpoint protegido)
  - [ ] `updateMany`: PENDENTE → ATRASADO onde `dataVencimento < hoje`
  - [ ] Executar diariamente (6h da manhã)
- [ ] Filtro rápido (AC: 6)
  - [ ] Botão "Inadimplentes" no header da aba de lançamentos
  - [ ] Filtra para mostrar apenas ATRASADO
- [ ] Exportação CSV (AC: 7)
  - [ ] Botão "Exportar CSV" na aba de lançamentos
  - [ ] `GET /api/lancamentos/export` com filtros aplicados
  - [ ] Colunas: cliente, descrição, valor, vencimento, status, data pagamento

## Dev Notes
**Depende de:** Story 6.1 (página financeiro), Story 1.4 (sidebar)

**CSV export:**
```ts
const csv = [
  'Cliente,Descrição,Valor,Vencimento,Status',
  ...lancamentos.map(l => `${l.cliente},${l.descricao},${l.valor},${l.dataVencimento},${l.status}`)
].join('\n')

return new Response(csv, {
  headers: {
    'Content-Type': 'text/csv',
    'Content-Disposition': 'attachment; filename="lancamentos.csv"'
  }
})
```

**Cron Vercel:** Usar `vercel.json` com `crons` config.

### Testing
- Testar badge atualiza após pagamento
- Testar cron atualiza PENDENTE → ATRASADO
- Testar export CSV contém todos os campos

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
