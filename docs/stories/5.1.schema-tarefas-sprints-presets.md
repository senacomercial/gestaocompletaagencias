# Story 5.1: Schema — Tarefas, Sprints, Detalhamentos & Presets

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["prisma:migrate", "typecheck"]

## Story
**As a** sistema,
**I want** os models de banco para tarefas, sprints, detalhamentos e presets de serviço,
**so that** o módulo de gestão de projetos tenha estrutura de dados completa.

## Acceptance Criteria
1. Model `Sprint`: id, projetoId (FK), nome, dataInicio, dataFim, organizacaoId
2. Model `Tarefa`: id, titulo, descricao, projetoId (FK), sprintId (FK nullable), responsavelId (FK), status enum (PENDENTE, EM_ANDAMENTO, CONCLUIDA, BLOQUEADA), prazo, ordem, organizacaoId, createdAt, updatedAt
3. Model `Detalhamento`: id, tarefaId (FK), conteudo (Text), autorId (FK), createdAt, updatedAt
4. Model `PresetServico`: id, nome, organizacaoId
5. Model `PresetTarefa`: id, presetServicoId (FK), titulo, descricao, ordemPadrao
6. API routes CRUD para todos os models
7. Seed com 2 presets: "Social Media" (8 tarefas), "Tráfego Pago" (6 tarefas)

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar model Sprint (AC: 1)
  - [ ] Campos: id, projetoId (FK→Projeto), nome, dataInicio, dataFim, organizacaoId, createdAt
  - [ ] Index em projetoId
- [ ] Criar model Tarefa (AC: 2)
  - [ ] Enum `StatusTarefa`: PENDENTE, EM_ANDAMENTO, CONCLUIDA, BLOQUEADA
  - [ ] Campos completos conforme AC
  - [ ] FK para Sprint (nullable), Projeto, Usuario (responsavel)
  - [ ] Index em projetoId, sprintId, responsavelId
- [ ] Criar model Detalhamento (AC: 3)
  - [ ] Campos: id, tarefaId (FK→Tarefa), conteudo (Text), autorId (FK→Usuario), createdAt, updatedAt
- [ ] Criar models de Preset (AC: 4, 5)
  - [ ] `PresetServico`: id, nome, organizacaoId
  - [ ] `PresetTarefa`: id, presetServicoId (FK), titulo, descricao, ordemPadrao
- [ ] Aplicar migration (AC: 6)
  - [ ] `prisma migrate dev --name projetos-tarefas`
- [ ] Criar API routes (AC: 6)
  - [ ] `/api/sprints` — CRUD
  - [ ] `/api/tarefas` — CRUD + PATCH status
  - [ ] `/api/detalhamentos` — GET by tarefa + POST
  - [ ] `/api/presets` — CRUD presets e suas tarefas
- [ ] Atualizar seed com presets (AC: 7)
  - [ ] "Social Media": Criação de conteúdo, Agendamento, Stories, Feed, Reels, Análise, Relatório, Reunião
  - [ ] "Tráfego Pago": Setup campanhas, Criação de anúncios, Otimização, Relatório semanal, Reunião, Análise de concorrentes

## Dev Notes
**Schema resumido:**
```prisma
enum StatusTarefa { PENDENTE EM_ANDAMENTO CONCLUIDA BLOQUEADA }

model Sprint {
  id            String   @id @default(cuid())
  projetoId     String
  nome          String
  dataInicio    DateTime
  dataFim       DateTime
  organizacaoId String
  tarefas       Tarefa[]
  projeto       Projeto  @relation(fields: [projetoId], references: [id])
  @@index([projetoId])
}

model Tarefa {
  id            String       @id @default(cuid())
  titulo        String
  descricao     String?
  projetoId     String
  sprintId      String?
  responsavelId String
  status        StatusTarefa @default(PENDENTE)
  prazo         DateTime?
  ordem         Int          @default(0)
  organizacaoId String
  // relations...
}
```

**Depende de:** Story 4.1 (model Projeto já criado)

### Testing
- Verificar migration aplica sem conflitos
- Verificar seed cria presets completos
- Testar CRUD de tarefas via API

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
