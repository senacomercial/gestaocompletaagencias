# Story 1.5: Dashboard Principal (KPIs Base)

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "unit-tests"]

## Story
**As a** gestor,
**I want** visualizar os principais indicadores da agência na tela inicial,
**so that** eu tenha visão geral do negócio ao fazer login.

## Acceptance Criteria
1. Dashboard em `/dashboard` com KPI cards: VGV Total, MRR, Leads Ativos, Tarefas do Dia
2. Cards com design dark, valor em dourado `#D4AF37` e label em texto muted
3. Valores buscados de queries reais no banco
4. Seção "Atividade Recente" com últimas ações
5. Seção "Tarefas do Dia" listando tarefas com prazo para hoje do usuário logado
6. Skeleton loading state enquanto dados carregam
7. Página completamente responsiva

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual.

## Tasks / Subtasks
- [ ] Criar página do dashboard (AC: 1)
  - [ ] Criar `src/app/(dashboard)/dashboard/page.tsx`
  - [ ] Grid de 4 KPI cards no topo
  - [ ] Títulos: VGV Total, MRR, Leads Ativos, Tarefas do Dia
- [ ] Criar componente KPI Card (AC: 2)
  - [ ] Criar `src/components/dashboard/KpiCard.tsx`
  - [ ] Props: title, value, subtitle, icon, trend
  - [ ] Design: fundo `#1A1A1A`, valor em `#D4AF37`, label em `#A1A1A1`
  - [ ] Borda sutil `#2A2A2A`
- [ ] Implementar queries para os KPIs (AC: 3)
  - [ ] Criar `src/app/api/dashboard/kpis/route.ts`
  - [ ] VGV Total: `SUM(lead.vgvTotal) WHERE status=VENDA_REALIZADA AND org=X` (placeholder 0 por ora se models não existem)
  - [ ] MRR: `SUM(contrato.recorrenciaMensal)` (placeholder)
  - [ ] Leads Ativos: COUNT de leads com status=ATIVO
  - [ ] Tarefas do Dia: COUNT de tarefas com prazo=hoje do usuário
- [ ] Criar seção Atividade Recente (AC: 4)
  - [ ] Criar `src/components/dashboard/RecentActivity.tsx`
  - [ ] Listar últimas 5 ações (leads criados, vendas, tarefas etc.)
  - [ ] Ícone, descrição, timestamp relativo (ex: "há 2h")
- [ ] Criar seção Tarefas do Dia (AC: 5)
  - [ ] Criar `src/components/dashboard/TodayTasks.tsx`
  - [ ] Query: tarefas do usuário com prazo = hoje
  - [ ] Exibir: título, projeto, status badge
  - [ ] Estado vazio: "Nenhuma tarefa para hoje 🎉"
- [ ] Implementar skeleton loading (AC: 6)
  - [ ] Usar `shadcn/ui Skeleton` em todos os componentes
  - [ ] Loading state enquanto dados carregam (Suspense ou useState)
- [ ] Garantir responsividade (AC: 7)
  - [ ] Grid de KPIs: 4 cols (desktop) → 2 cols (tablet) → 1 col (mobile)
  - [ ] Seções inferiores empilhadas em mobile

## Dev Notes
**Nota:** Esta story é a base do dashboard. Os KPIs de VGV, MRR e leads completos serão populados pelas Epics 2, 4 e 6. Por ora, queries retornam 0 se os models ainda não existem — usar try/catch silencioso.

**Padrão de query (Server Component):**
```ts
// src/app/(dashboard)/dashboard/page.tsx
import { getServerSession } from 'next-auth'
import { prisma } from '@/lib/prisma'

export default async function DashboardPage() {
  const session = await getServerSession()
  const orgId = session?.user?.organizacaoId
  // queries aqui...
}
```

**Skeleton pattern:**
```tsx
import { Skeleton } from '@/components/ui/skeleton'
// <Skeleton className="h-8 w-32" />
```

### Testing
- Verificar que dashboard carrega sem erros com banco vazio
- Verificar skeleton aparece durante loading
- Testar grid responsivo em diferentes viewports

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-23 | 1.0 | Story criada | Orion (@aios-master) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
